/**
 * Naming Conventions
 *
 * 1) 일반적으로 영어(라팅문자)를 사용하며 문자와 숫자를 모두 사용할 수 있다.
 * 2) 특수기호는 언더스코어와 달러를 사용할 수 있다.
 * 3) 숫자로 이름을 시작할 수 없다.
 * 4) 키워드는 변수명으로 사용할 수 없다. ex) var const = "no"
 */

let name = "윤장호";
var $aveMoney = "돈";
const _count = 0;

console.log(name);
console.log($aveMoney);
console.log(count);

// let 1name = "no";
// let const = "no";

/**
 * Naming Conventions 2
 *
 * 1) camelCase - 대부분의 언어에서 많이 사용
 * 2) snake_case - 파이썬의 경우에 주로 사용
 * 3) PascalCase - C# 같은 마이크로소프트 계열의 언어에서 사용
 */

const javaScript = "helloWorld";
console.log(javaScript);
